# dev/test only — the game itself needs no build step
node_modules/
package-lock.json

# headless QA output
tools/shots/

# misc
.DS_Store
*.log

# secrets — never commit API keys (MESHY_API_KEY etc.)
.env
.env.*
!.env.example
.claude/settings.local.json
