# Meshy MCP server credentials — copy to `.env` (git-ignored) and fill in.
# Get an API key at https://www.meshy.ai → Settings → API. It starts with "msy_".
# The .mcp.json config reads this variable; the key is never committed.
MESHY_API_KEY=msy_your_key_here
